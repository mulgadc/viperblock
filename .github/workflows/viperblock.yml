name: Viperblock Build and Test

on:
  push:
    branches:
      - main
      - dev
    paths:
      - "**/*.go"
      - go.mod
      - go.sum
      - Makefile
      - .github/workflows/viperblock.yml
  workflow_dispatch:

jobs:
  build_and_test:
    name: Viperblock Build and Test
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout Code
        uses: actions/checkout@v6

      - name: Install nbdkit dependencies
        run: sudo apt-get update && sudo apt-get install -y nbdkit nbdkit-plugin-dev pkg-config

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          cache-dependency-path: "go.sum"
          go-version-file: "go.mod"

      - name: Build Viperblock
        run: make build

      - name: Run Tests
        run: make test

  lint_and_security:
    name: Viperblock Lint and Security
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout Code
        uses: actions/checkout@v6

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          cache-dependency-path: "go.sum"
          go-version-file: "go.mod"

      - name: Check Format
        run: make check-format

      - name: Check Modernize
        run: make check-modernize

      - name: Go Vet
        run: make vet

      - name: Security Checks
        run: make security-check
